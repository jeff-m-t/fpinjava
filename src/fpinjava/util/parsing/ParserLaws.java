package fpinjava.util.parsing;

import fpinjava.test.Gen;
import fpinjava.test.Prop;

import static fpinjava.data.Either.*;

public class ParserLaws {
//	private static <A> Prop equal(Parser<A> p1, Parser<A> p2, Gen<String> in) {
//		return Prop.forAll(in, s -> run(p1,s).equals(run(p2,s)));
//	}
//	
//	public static <A> Prop mapLaw(Parser<A> pa, Gen<String> in) {
//		return equal(pa, pa.map(a -> a), in);
//	}
//	
//	public static <A> Prop succeedLaw(A a, Gen<String> in) {
//		return Prop.forAll(in, s -> run(succeed(a),s).equals(right(a)));
//	}
}
